import React from "react";
import { StyleSheet, View } from "react-native";

const Order = () => {
    return (
        <Box>
            <Grid>
                <TextField>Order</TextField>
            </Grid>
        </Box>
    );
};

const styles = StyleSheet.create({});

export default Order;
